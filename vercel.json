{
  "installCommand": "git config --global url.\"https://${REPO_TOKEN}@github.com/\".insteadOf \"https://github.com/\" && git submodule update --init --recursive && npm install",
  "buildCommand": "pip install pillow && npm run build:frontend",
  "outputDirectory": "apps/frontend/dist"
}
